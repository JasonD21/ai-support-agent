name: heartbeat
on:
  schedule:
    - cron: "*/11 * * * *"
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: curl -fsS https://<render-app>.onrender.com/health || true
